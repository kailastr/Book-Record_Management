const UserModel = require("./UserModel");
const BookModel = require("./BookModel");

module.exports = { UserModel, BookModel };

//the purpose of this page is to help the importing of both the books and user models in one single line